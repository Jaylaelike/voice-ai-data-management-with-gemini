# 🚀 Quick Start Guide - เริ่มใช้งานภายใน 3 นาที

## ขั้นตอนที่ 1: ดาวน์โหลดโปรเจ็กต์

```bash
# ถ้ามีไฟล์อยู่แล้ว ข้ามขั้นตอนนี้
# ถ้ายังไม่มี สามารถ copy ไฟล์ทั้ง 3 ไฟล์:
# - index.html
# - app.js  
# - README.md
```

## ขั้นตอนที่ 2: รับ Gemini API Key (ฟรี!)

### 2.1 ไปที่เว็บไซต์
เปิดเบราว์เซอร์ไปที่: https://makersuite.google.com/app/apikey

### 2.2 Login
- ล็อกอินด้วย Google Account

### 2.3 สร้าง API Key
- คลิก "Create API Key"
- เลือก "Create API key in new project" (หรือเลือก project ที่มีอยู่)
- คัดลอก API Key ที่ได้

**ตัวอย่าง API Key:**
```
AIzaSyABC123DEF456GHI789JKL012MNO345PQR678STU
```

## ขั้นตอนที่ 3: รันโปรเจ็กต์

### วิธีที่ 1: เปิดไฟล์โดยตรง (ง่ายที่สุด!)

```bash
# Windows
start index.html

# macOS  
open index.html

# Linux
xdg-open index.html
```

### วิธีที่ 2: ใช้ Python (แนะนำ)

```bash
# เปิด Terminal/Command Prompt ในโฟลเดอร์โปรเจ็กต์
cd thai-voice-chat

# รัน HTTP Server
python -m http.server 8000

# เปิดเบราว์เซอร์ไปที่
# http://localhost:8000
```

### วิธีที่ 3: VS Code Live Server

```bash
# 1. เปิดโปรเจ็กต์ใน VS Code
# 2. ติดตั้ง Extension "Live Server"
# 3. คลิกขวาที่ index.html
# 4. เลือก "Open with Live Server"
```

## ขั้นตอนที่ 4: ใช้งาน

### 4.1 ใส่ API Key
1. เปิดแอปในเบราว์เซอร์
2. เลื่อนลงไปด้านล่าง
3. ใส่ Gemini API Key ในช่อง "Gemini API Key"
4. API Key จะถูกบันทึกอัตโนมัติ

### 4.2 ทดสอบ Voice Chat
1. **กดปุ่มไมค์** 🎤 (ปุ่มสีม่วงกลางจอ)
2. เบราว์เซอร์จะขออนุญาตใช้ไมโครโฟน → **กด "Allow"**
3. พูด: **"สวัสดีครับ"**
4. AI จะตอบกลับทั้งข้อความและเสียง!

### 4.3 ทดสอบ Text Chat
1. พิมพ์ในช่องข้อความ: **"อธิบายเกี่ยวกับ AI"**
2. กด **Enter** หรือปุ่ม **"ส่ง"**
3. AI จะตอบและอ่านคำตอบออกเสียง

## 🎯 ตัวอย่างคำถามที่ลองได้

- "อธิบายเกี่ยวกับ Gemini AI"
- "สอนภาษาอังกฤษให้หน่อย"
- "เล่านิทานให้ฟังหน่อย"
- "แนะนำสูตรอาหารไทย"
- "สรุปข่าววันนี้"

## ⚙️ การตั้งค่าเบราว์เซอร์

### อนุญาตไมโครโฟน (Chrome/Edge)

1. คลิกไอคอนกุญแจหรือ "ⓘ" ข้างซ้าย URL bar
2. หา "Microphone" หรือ "ไมโครโฟน"  
3. เปลี่ยนเป็น "Allow" หรือ "อนุญาต"
4. Reload หน้าเว็บ

### แก้ปัญหาเสียงไม่ออก

1. ตรวจสอบว่าลำโพงหรือหูฟังเปิดอยู่
2. เช็ค volume ในเบราว์เซอร์
3. ลอง refresh หน้าเว็บ
4. เปลี่ยนเสียง TTS (บางเบราว์เซอร์ต้องดาวน์โหลดเสียงภาษาไทย)

## 🆘 Troubleshooting

### ❌ "เบราว์เซอร์ไม่รองรับการรับรู้เสียง"
**แก้:** ใช้ Chrome หรือ Edge (Safari บน macOS/iOS ใช้ได้บางฟีเจอร์)

### ❌ "Failed to get response" หรือ Error 400
**แก้:** 
- ตรวจสอบ API Key ว่าถูกต้อง
- ตรวจสอบว่ามีอินเทอร์เน็ต
- ลอง generate API Key ใหม่

### ❌ ไมค์ไม่ทำงาน
**แก้:**
- อนุญาตการใช้งานไมโครโฟนในเบราว์เซอร์
- ตรวจสอบว่าไมค์เปิดอยู่และทำงานปกติ
- ลองปิด-เปิดเบราว์เซอร์ใหม่

### ❌ เสียงตอบกลับไม่ออก
**แก้:**
- ตรวจสอบ volume เบราว์เซอร์และระบบ
- บางเบราว์เซอร์ต้องมี user interaction ก่อน (คลิกอะไรสักอย่างในหน้าเว็บ)
- ลอง refresh หน้า

## 💰 ค่าใช้จ่าย

Gemini API มี **Free Tier** ที่ใช้งานได้:
- 15 requests/minute
- 1,500 requests/day
- เหมาะสำหรับทดสอบและใช้งานส่วนตัว

ดูรายละเอียด: https://ai.google.dev/pricing

## 📞 ติดต่อ/ขอความช่วยเหลือ

หากมีปัญหาหรือคำถาม:
1. ตรวจสอบ Console (กด F12 ในเบราว์เซอร์)
2. อ่าน error message
3. ลองแก้ตามคำแนะนำข้างบน

---

**สนุกกับการใช้งาน Thai Voice Chat! 🎉**
